# Shell scripts and hooks must keep LF endings regardless of platform —
# a CRLF-corrupted husky hook fails with "$'\r': command not found" on Linux.
* text=auto eol=lf

*.sh text eol=lf
.husky/* text eol=lf
