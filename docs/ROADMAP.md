# LinguaAI — Product Roadmap

Status: **v1.1 — Consolidated baseline** · Owner: CPO · Last updated: 2026-07-29

Supersedes Draft v1.0. See [BASELINE.md](BASELINE.md) for the current authoritative summary. This roadmap sequences the 30 product modules from PRD.md §6, plus the additional capabilities identified during the Architecture Review Gate, across three phases and 23 MVP-phase epics. Phase boundaries are defined by product/business readiness, not fixed calendar dates — each phase has explicit exit criteria.

## Phase 1 — MVP (Foundation & Core Learning Loop)

**Goal:** Prove the core value proposition — an AI teacher that assesses, personalizes, and coaches — with a sustainable unit economics model, on web first.

**In scope:**

- Module 1 — User Identity Platform (email + social auth, RBAC, mandatory admin MFA — ADR-011)
- Module 2 — AI Language Assessment Engine
- Module 3 — Personalized Learning Engine
- Module 4 — AI Teacher Platform (all 7 agents, memory-enabled, single-Orchestrator handoff protocol — ADR-007, RAG-grounded — ADR-008)
- Module 5 — Course Management System (including AI-assisted content authoring)
- Module 6 — Vocabulary Intelligence System
- Module 7 — Speaking Practice System
- Module 8 — Pronunciation Lab
- Module 9 — Listening System
- Module 10 — Reading System
- Module 11 — Writing Assistant
- Module 12 — AI Story Generator
- Module 15 — Gamification Engine (core: XP, streaks, levels, badges, anti-gaming safeguards)
- Module 19 — Exam Preparation (1–2 exam programs at launch, RAG-grounded scoring)
- Module 21 — Certificate System (basic completion certificates)
- Module 22 — Subscription Platform (Free, Premium via Stripe, including trial and cancellation/downgrade flows)
- Module 23 — Analytics Platform (internal, including CEFR-progression outcome measurement)
- Module 24 — Admin Platform (content & user management essentials)
- Module 25 — Notification System (email + push, granular preferences)
- Module 26 — Security System (full — non-negotiable at every phase; RLS multi-tenancy, mandatory admin MFA)
- Module 28 — Mobile Application (Flutter, core learning loop, last-write-wins offline sync)
- Module 29 — AI Infrastructure (gateway, agents, RAG, memory, model management, cost circuit breaker — ADR-012)
- Module 30 — Internal Platform Services (logging, monitoring, jobs, queues, domain events)

**Deferred out of MVP:**

- Module 13 — AI Translation Camera → Growth
- Module 14 — Video Learning → Growth
- Module 16 — Community Platform → Growth (basic friends/leaderboards may land late-MVP if capacity allows)
- Module 17 — AI Avatar Teacher → Enterprise/Future
- Module 18 — Teacher Marketplace → Growth (`TEACHER` role exists at MVP with scoped, non-monetized capability — PRD.md §5.1)
- Module 20 — Enterprise LMS → Enterprise
- **Module 22 — Family plan → Version 2, blocked on a fully specified COPPA parental-consent flow (ADR-013); Business plan → Enterprise**
- Module 27 — Public API Platform → Future
- Referral/viral growth program, gift subscriptions, cosmetic in-app purchases → Version 1.1 (PRD.md §7)

**Exit criteria (see PRD.md §8 for full detail):**

- Assessment completion rate ≥ 70%.
- Voice conversation latency within budget (PERFORMANCE.md §2).
- 10 launch target languages with complete A1–B2 content, 1 launch UI language (English).
- Zero open P0/P1 security findings; all 8 Architecture Review blockers closed (see BASELINE.md).
- Positive or credibly-trending-positive unit economics (AI cost per active user vs. Premium conversion, PRD.md §7).

## MVP implementation epics

23 epics sequence the MVP phase. Full detail (objective, business value, dependencies, complexity, acceptance criteria, risks, deliverables) for each is maintained here as the living, current source — the original Architecture Review draft of this sequence (`ARCHITECTURE_REVIEW.md`, now archived) is preserved for historical reference only.

| --- | ---------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1 | [Foundation & Engineering Platform Bootstrap](epics/E1-foundation-platform-bootstrap.md) | L | None (first epic) — **[remediated](epics/E1-remediation-report.md) after NO GO from [independent review](epics/E1-production-readiness-review.md); second review returned GO; T1–T26 implementation complete, see [acceptance report](epics/E1-acceptance-report.md) — never yet committed/pushed or exercised through a real CI/CD run** |
| E2 | [Identity & Access Platform](epics/E2-identity-access-platform.md) | L | E1 — **implementation complete (T1–T29); ADR-018–023 accepted; independent [post-implementation acceptance review](epics/E2-final-acceptance-review.md) returned CONDITIONAL ACCEPTANCE (3 blocking findings), [remediated](epics/E2-final-acceptance-remediation.md) — READY FOR TARGETED FINAL RE-VERIFICATION, not yet formally closed (see epic doc's gate sign-off log)** |
| E3 | Design System & Component Library v1 | M | E1 |
| E4 | [Database Schema & Core Data Layer](epics/E4-database-schema-core-data-layer.md) | L | E1 — **implementation complete (T1–T12, 2026-08-07), by explicit user direction rather than an independent Architecture Gate review; §12's gate sign-off log remains unchecked, not yet formally closed** |
| E5 | [AI Gateway & Agent Orchestration Core](epics/E5-ai-gateway-agent-orchestration-core.md) | XL | E1, E4 — **implementation complete (T1–T12, 2026-08-07), by explicit user direction rather than an independent Architecture Gate review; §12's gate sign-off log remains unchecked, not yet formally closed** |
| E6 | [AI Language Assessment Engine](epics/E6-ai-language-assessment-engine.md) | L | E4, E5 — **implementation complete (T1–T8, 2026-08-07): AssessmentItem item bank (ADR-037); apps/api AssessmentModule attempt lifecycle, adaptive item selection, objective scoring (ADR-038); CEFR banding/confidence + ProficiencyLevel/History writes; ai-engine AssessmentScoringService for Writing-skill AI scoring (ADR-039); apps/api↔ai-engine REST contract for Writing scoring (E5 T9–T12 merged in to unblock this); `assessment.attempt.completed` event emission, user-initiated re-assessment flow, low-confidence retake-offer contract; WRITING wired into the live session (AI-scored via ai-engine) and the full assessment-attempt API_SPEC_TEMPLATE.md instance; interim golden-set-style evaluation suite for Writing-skill AI scoring (AI_GOVERNANCE.md §3), by explicit user direction rather than an independent Architecture Gate review; §12's gate sign-off log remains unchecked, not yet formally closed** |
| E7 | [Personalized Learning Engine](epics/E7-personalized-learning-engine.md) | L | E4, E6 — **implementation complete (T1–T5, 2026-08-08 — the epic's own full §9 task sequence, confirmed no further task remains): `recommendation-engine`'s first real Prisma access (ADR-040, `app_role` via `APP_DATABASE_URL`); `LearningPlan` generation on `assessment.attempt.completed` via the platform's first real BullMQ event consumer (real fan-out/retry-policy gap found and tracked, RISK_REGISTER R-89); `DailyGoal` nightly generation + weakness-detection scoring, both epic's own open design questions (timezone-correct date, weakness thresholds) resolved during implementation, plus a real BullMQ Worker/Queue unhandled-'error'-event production bug found, fixed, and flagged as also present unfixed in `ai-engine` (RISK_REGISTER R-90); new `recommendation.daily_goal.ready` event cataloged and emitted — `recommendation-engine`'s first real event producer, which surfaced and fixed a real `producedBy`-hardcoding correctness bug in the shared `packages/events` publisher; T5's `apps/api` read contract (`GET /v1/learning-plans/current`, `GET /v1/daily-goals/today`), resolving a real multi-language `LearningPlan` scoping gap and the epic's own named timezone-correctness open question, and surfacing (but not fixing, out of scope) 7 pre-existing E2/Identity e2e failures unrelated to this epic (RISK_REGISTER R-91) — verified against live local Postgres/Redis, by explicit user direction rather than an independent Architecture Gate review; §12's gate sign-off log remains unchecked, not yet formally closed** |
| E8 | [Course Management System](epics/E8-course-management-system.md) | XL | E4 — **implementation complete (T1–T4, 2026-08-08 — the epic's own full §9 task sequence, confirmed no further task remains): admin CRUD + publish/versioning workflow for the full Course/Level/Unit/Lesson/Activity/Quiz/Exercise hierarchy (`apps/api`'s new `CourseModule`, `ADMIN`-only), real `ContentVersion` snapshotting proven — via a real e2e test against live Postgres — to survive a post-publish edit without retroactively changing what an already-recorded `ExerciseAttempt` was pinned to; two real build-config gaps found and fixed along the way (a stale `tsconfig.cjs.json` Node10 `paths` override, a stale vitest coverage-exclusion comment); T2 adds the learner-facing published-only read API and a real exercise-attempt submission endpoint with a deterministic scorer for every objective `ExerciseType`, each attempt really pinned to the exercise's own current `ContentVersion`, `correctAnswer` never leaked; T3 adds the first real emission of `learning.exercise.answered`/`learning.lesson.completed` (both already cataloged, never produced until now), resolving the epic's own open score-aggregate-formula question and excluding never-attemptable `SPEAKING_PROMPT` exercises from the completion check; T4 (ADR-041) adds AI-assisted content drafting — a new `services/ai-engine` `ContentDraftingService` under a third `AiRequestClass` (`'content'`, which required refactoring `RouterService.modelFor()`'s binary ternary into a scalable switch-based lookup), a schema-level exclusion of the never-attemptable `SPEAKING_PROMPT` type from generated drafts, and an `ADMIN`-only `apps/api` review endpoint that returns a proposal and persists nothing — proven via a live e2e assertion of zero created `Lesson` rows — verified against live local Postgres/Redis throughout, by explicit user direction rather than an independent Architecture Gate review; §12's gate sign-off log remains unchecked, not yet formally closed** |
| E9 | [Vocabulary Intelligence (SRS)](epics/E9-vocabulary-intelligence-srs.md) | M | E4, E8 — **implementation complete (T1-T4, 2026-08-08 — the epic's own full §9 task sequence, confirmed no further task remains): curated `VocabularyItem` catalog and a per-user personal dictionary are both live; the SM-2-derivative SRS review engine is live too (ADR-042, accepted — deliberately built in `apps/api`, not `recommendation-engine`, despite ARCHITECTURE.md §2.1 naming it by name as that service's own example, since flashcard review is synchronous and `recommendation-engine`'s real code is exclusively asynchronous with no frontend-facing HTTP surface) — idempotent add-to-deck, a due-cards query, and a review-submission endpoint applying a pure, exhaustively-tested SM-2 transition function; T4 adds AI-assisted vocabulary-item drafting, extending E8 T4's own `ContentDraftingService`/`'content'` `AiRequestClass` rather than a new one, never auto-published — a real orphaned-test-data environment issue was found, diagnosed, and cleaned up along the way, with the e2e "never persists" assertion hardened against the same class of pollution recurring** |
| E10 | [Speaking Practice & Speech Pipeline](epics/E10-speaking-practice-speech-pipeline.md) | XL | E5 — **implementation complete (T1-T7, 2026-08-12 — the epic's own full §9 task sequence, confirmed no further task remains): `services/speech-service`'s first real domain code — `SttProvider`/`TtsProvider` adapters (`whisper-1`/`tts-1`, ADR-043), the hand-rolled HMAC internal-token mechanism shared with `apps/api` (T1); `apps/api`'s `SpeakingModule` minting that token via `POST`/`DELETE /v1/speaking-sessions` (T2, closed a real session-ownership security gap along the way); the platform's first real-time WebSocket transport — raw `ws` + manual HTTP upgrade, pre-handshake auth (T3, ADR-045); T4 (ADR-047) closes the full round trip — `ai-engine`'s contract extended with `audioUrl`/`messageId`, a new `PATCH .../audio-url` endpoint, `speech-service`'s own `AiEngineClientService` consuming the SSE turn live (`ai.token`/`ai.done` relayed, each completed sentence synthesized and streamed as `speech.audio-chunk`), and this platform's **first real object-storage consumer** (`S3AudioStorageProvider`, `@aws-sdk/client-s3`, against the already-configured-since-E1 S3/MinIO target) — a real concurrency bug (duplicate SSE consumption) was found via a failing test and fixed before shipping. Two real, honestly-flagged gaps tracked as RISK_REGISTER R-93 (stored URLs aren't yet servable without a future signed-URL layer; no malware scanning on upload). T5 (ADR-048) adds session-end scoring — a fourth `AiRequestClass` (`'fluency'`), a new `FluencyScoringService` reading the session's own persisted transcript by `sessionId` (idempotent replay, legitimate no-op on an empty transcript), real `FluencyScore` writes and `PersonalDictionary` extraction into E9's own service (its first cross-module export), and a refinement — not a duplication — of `EVENT_ARCHITECTURE.md`'s own pre-existing, never-implemented `speech.session.ended` placeholder row. Confirmed, deeper than previously known: ai-engine's own e2e suite cannot boot at all in this credential-less environment (RISK_REGISTER R-88), even for its pre-existing health check — unit-tested instead, matching this session's own established convention. T6 (§6.5, AI_GOVERNANCE.md §7) adds real graceful degradation — a sticky, connection-lifetime `degraded` flag on `SpeechSessionConnection`, a new `speech.degraded` server message sent at most once, an STT or TTS failure notifying the client and switching to text-only rather than ending the session, proven live by an e2e test forcing a real stubbed TTS failure mid-session and showing a second, ordinary turn afterward still gets a real reply. A real, honestly-flagged gap found along the way (RISK_REGISTER R-94): the realtime protocol has no client→server text message of its own, so a session degraded by an STT failure specifically has no way to receive further turns, only to be honestly told why — a real, out-of-scope protocol change tied to a future client UI. T7 (§6.5, API_GUIDELINES.md §9) closes the epic — a `sessionId`-keyed reconnection grace window (60s) lets a genuinely dropped WebSocket resume the same in-flight turn rather than losing it, proven live by an e2e test showing audio sent across two different physical sockets gets transcribed as one continuous turn; a drop that's never resumed marks the session `ABANDONED` via a new `ai-engine` endpoint, the first real writer of that enum value through E1-T6. RISK_REGISTER R-04's own named provider-latency spike still cannot be executed in this credential-less environment; design not yet formally approved, proceeding by explicit user direction** |
| E11 | [Pronunciation Lab](epics/E11-pronunciation-lab.md) | L | E10 — **implementation complete (T1-T2, 2026-08-13 — the epic's own full §9 task sequence, confirmed no further task remains): `services/speech-service`'s second independent speech-provider integration — a new `PronunciationProvider` adapter and `AzurePronunciationAssessmentProvider` (ADR-049), since OpenAI's `whisper-1` cannot produce phoneme-level scores; a new, stateless `POST /v1/pronunciation/score`, no persistence, no session state. `apps/api`'s new `PronunciationModule` (T2) — its first-ever direct HTTP client to `speech-service` (`SpeechServiceClientService`), real `PronunciationLabAttempt`/`PronunciationScore` writes closing `ai.prisma`'s own header-flagged gap since E4 T5, and the new `pronunciation.attempt.scored` domain event (ADR-050). A real, found-and-closed gap: `Language.code` is a plain 2-letter code but Azure needs a full BCP-47 locale — closed with a small, explicit, honestly-scoped map (throws rather than guesses on an unmapped code). Two genuinely structural gaps found and tracked, not silently absorbed (RISK_REGISTER R-95/R-96): no persona's specialist-tool trigger has ever been wired to fire for real, and no entitlement-enforcement guard exists anywhere in this platform yet despite PRD.md naming this a Premium-only feature (E15's own future scope). A real regression (T1) was found and fixed before shipping: wiring the new provider module broke every existing `speech-service` e2e test until `.env.e2e` got the two new required vars. Verified against live local Postgres throughout; design not yet formally approved, proceeding by explicit user direction** |
| E12 | [Listening & Reading Systems](epics/E12-listening-reading-systems.md) | M | E5, E8 — **implementation complete (T1-T2, 2026-08-13 — the epic's own full §9 task sequence, confirmed no further task remains): needs no new external provider, as designed — `packages/validation`'s `content` module now real-validates `LISTENING`/`READING` `Activity.content` (three distinctly-scoped Zod `superRefine` functions for the create-request, AI-draft, and persisted shapes, closing `content.prisma`'s own long-unkept "validated at the application layer" promise for these two `ActivityType`s); `services/speech-service`'s new stateless `POST /v1/speech/synthesize` (ADR-051) reuses the already-integrated `TtsProvider` and does its own S3 upload via its already-injected `AudioStorageProvider`, returning a real, ready-to-use `audioUrl` — a genuine, better design than this epic's own original draft, which wrongly assumed `apps/api` would do the upload itself; `apps/api`'s `LessonContentService.createActivity()` synthesizes a drafted `LISTENING` script into real, persisted audio server-side before the row is ever written. T2 adds the learner-facing `GET /v1/reading-activities/matched` — published `READING` activities ordered by nearest-CEFR-band distance to the caller's own `ProficiencyLevel` (defaulting to `A1` for a never-assessed learner), sorted in application code since `content.cefrLevel` lives inside an unindexed JSON column. Verified against live local Postgres/Redis throughout (real e2e tests prove a genuinely uploaded `audioUrl` on a persisted `Activity.content` row, and the real nearest-CEFR ordering including a draft-course exclusion check)** |
| E13 | [Writing Assistant & AI Story Generator](epics/E13-writing-assistant-story-generator.md) | L | E5, E9 — **implementation complete (T1-T3, 2026-08-13 — the epic's own full §9 task sequence, confirmed no further task remains): `RagRetrievalService` (E5 T7) gets its first real consumer — a new `'writing'` `AiRequestClass` (ADR-052) and `WritingCoachService`/`POST /v1/writing-coaching/correct` (`services/ai-engine`), RAG-grounded against `KnowledgeBaseEntry`'s `GRAMMAR_REFERENCE` category. A real gap found and closed for real (ADR-053): zero `GRAMMAR_REFERENCE` rows existed anywhere, and even the two pre-existing rows carried no real embedding — `packages/database/scripts/seed.ts` now computes genuine OpenAI embeddings for three real, illustrative Spanish grammar-reference entries, verified via a live pgvector retrieval check against local Postgres. T2 adds `apps/api`'s `WritingModule` (`POST /v1/writing-submissions`) — a new self-contained `WritingSubmission` model, corrected terms saved into the caller's own `PersonalDictionary` (new `WRITING` source), a new `writing.submission.corrected` domain event. T3 adds the AI Story Generator — `ContentDraftingService.draftStory()` (still `'content'` class, but shown directly to the learner and persisted immediately, unlike the review-gated lesson/vocabulary drafts), `POST /v1/stories` deriving its own target vocabulary from the caller's own `PersonalDictionary` (never caller-supplied), a new `GeneratedStory` model. Two real design gaps caught and fixed during implementation before shipping: the story's own CEFR level reuses E12 T2's own `ProficiencyLevel`-with-`A1`-default precedent rather than a new hardcoded guess, and a learner with no saved vocabulary yet gets a real, honest 400 rather than a meaningless fallback term. Verified against live local Postgres/Redis throughout** |
| E14 | [Gamification Engine](epics/E14-gamification-engine.md) | M | E4, E3 — **Complete (T1+T2, 2026-08-14). `GamificationService.recordActivity()` real, called synchronously in-process from `ExerciseAttemptsService` (ADR-054, avoids RISK_REGISTER R-89's competing-consumers trap rather than adding a second real BullMQ consumer). Real anti-gaming safeguard live: XP awarded only on a learner's first correct attempt at each exercise, closing PRD's own named launch-blocking farming vector. Timezone-correct streak grace window live via `toLocalCalendarDate()`. Badge-earning and mission-progress tracking real (T2) — added `UserXP.lessonsCompleted`, the persisted counter no table previously tracked, to evaluate the seeded `LESSONS_COMPLETED` criteria; missions accumulate by real per-call delta, not a lifetime snapshot; learners lazily self-enroll into active missions. `gamification.xp.awarded`/`streak.updated`/`badge.awarded` real emission; `GET /v1/gamification/me`/`badges`/`missions` all live. 48 unit tests (gamification module + `exercise-attempts.service.spec.ts`) + 7 e2e tests passing; full `apps/api` e2e suite (285 tests) re-run clean apart from 3 pre-existing, unrelated failures. Two real cross-test-file FK regressions found and fixed during verification (global Badge/Mission tables need cleanup scoped by badgeId/missionId, not just userId)** |
| E15 | [Subscription & Billing Platform](epics/E15-subscription-billing-platform.md) | L | E2, E4 — **T1+T2 done (2026-08-14). T1: real Stripe Checkout Session creation and real webhook signature verification + `Subscription`/`Invoice`/`Entitlement` sync, added `Plan.stripePriceId` (§3.2). Proves PRD's own "no paywall lag" requirement for real: a webhook-driven `Entitlement` change is synchronously visible on a same-flow `GET /v1/billing/me` read (e2e-verified, live Postgres). Two real bugs found and fixed: `Subscription`'s own existing RLS policy denies all writes to `app_role`, so webhook-driven writes needed the service-role (BYPASSRLS) client; the Stripe SDK's own constructor throws on an empty API key, crashing every `apps/api` e2e suite's own boot until a placeholder fallback was added. T2: real `EntitlementGuard`/`@RequireEntitlement()`, wired onto `PronunciationLabController` — a FREE-plan caller is rejected with 403, a PREMIUM-plan caller passes through, e2e-verified against live Postgres, partially closing RISK_REGISTER R-99's own entitlement-enforcement gap for its one concrete instance. A real gap found and fixed: guards run _before_ `TenantContextInterceptor` in Nest's own pipeline, so an `appPrisma`-backed read inside a guard has no RLS tenant context set yet (the same `invalid uuid: ''` bug class as T1's own webhook path) — fixed by adding `BillingService.hasEntitlement()`, which reads via the service-role client instead. 55 unit tests (billing + entitlement guard + pronunciation) + 14 e2e tests (8 billing + 6 pronunciation) passing across both tasks; full suite (295 tests) re-run clean apart from the same 3 pre-existing, unrelated failures. T3: a real BullMQ repeatable reconciliation job (hourly, `BillingModule`'s own Worker, mirrors `recommendation-engine`'s own `DailyGoalModule`) periodically re-syncs Stripe's own subscription list against Postgres, catching a dropped webhook — e2e-proven by enqueuing a real one-off job and confirming a real `Subscription`/`Entitlement` row lands, reliable 100% of the time in isolation. Redis-cached entitlement resolution (`EntitlementCacheService`, deliberately fail-open, synchronously invalidated on every real write) now backs `GET /v1/billing/me`/`EntitlementGuard` — the "no paywall lag" guarantee still holds by construction, re-confirmed by T1's own existing tests. A real, found concurrency limitation tracked as RISK_REGISTER R-100 (new): BullMQ workers are competing consumers across every `apps/api` e2e file's own independently-booted `AppModule`, so this suite's own one-off job can occasionally be raced away by an unrelated test file's own competing Worker under full-suite parallel load — a test-environment artifact, not an implementation defect. 56 unit tests + 1 new e2e test; full suite re-run shows no regression beyond the pre-existing baseline plus this newly-tracked, isolation-reliable flake. **Epic complete** |
| E16 | [Notification System](epics/E16-notification-system.md) | M | E4, E1 — **Epic complete, T1-T3 implemented and merged (2026-08-14). T1: `packages/events` per-consumer queue fan-out closes RISK_REGISTER R-89 for real (ADR-054); a real, found "Connection is closed" race fixed via `.waitUntilReady()`. T2: `services/notification-service`'s own real `NotificationModule` is live — a real `Worker` consumer on its own fan-out queue, real SMTP email delivery via `nodemailer`, `NotificationPreference` enforcement, `identity.user.registered`/`identity.password.reset_requested` wired end-to-end, real MailHog-verified e2e delivery proof. A real, load-bearing gap found and resolved: the password-reset event payload previously carried no way to build a working reset link — fixed by adding the raw, one-time token to the event payload, a deliberate, bounded trade-off tracked as RISK_REGISTER R-101. T3: real `GET`/`PUT /v1/notification-preferences` (`apps/api`) and `notification.preference.changed` emission — `GET` synthesizes the full default-opted-in set per §3.4, `PUT` rejects a `SECURITY_ALERT` opt-out attempt with 422 since the send path never honors it anyway (a real design decision made and enforced, not left silent). Only two of the ~11 catalog-named events are wired end-to-end and the actual preferences UI is out of scope — both real, tracked follow-ups (design doc §11), not oversights** |
| E17 | [Analytics Platform & Instrumentation](epics/E17-analytics-platform.md) | L | E4, E6 — **Epic complete, T1-T3 implemented and merged (2026-08-14). T1: `analytics-service` registered as the platform's third real domain-event consumer (ADR-055), with a deliberately generic (no per-event-type switch) ingestion consumer persisting every real, cataloged event type into `LearningEvent` — that table's own first real write path since E4 T10, and the platform's first real implementation of `EVENT_ARCHITECTURE.md` §4's own idempotency claim (tracked as RISK_REGISTER R-102 for the two existing consumers that still lack it). T2: real `GET /v1/admin/analytics/cefr-progression`/`ai-cost` — pure reporting layers over already-real `ProficiencyLevelHistory`/`AIUsageLog`, computing PRD.md §5.1's own named required CEFR-progression-rate deliverable as a real per-skill cohort aggregate; a real counting bug (a 2-record user whose level never changed was silently excluded from the denominator) was found and fixed before shipping. T3: real `GET /v1/admin/analytics/overview` — PRD.md §7's own named core business metrics (activation, D1/D7/D30 retention, Free→Premium conversion, AI cost per active user), all `ADMIN`-gated reusing `CourseModule`'s own established auth pattern throughout. The real, flagged MVP compromise on reporting-query placement against ARCHITECTURE.md §2 (no read replica exists anywhere in this platform yet) remains a tracked, deferred risk, not resolved by this epic** |
| E18 | Admin Platform | M | E2, E4, E8 |
| E19 | [Exam Preparation System](epics/E19-exam-preparation-system.md) | L | E5, E13 -- **Epic complete, T1-T3 implemented and merged (2026-08-14). T1: real `MockTestSection` model (ADR-057), `apps/api`'s new `ExamsModule` -- ADMIN authoring, learner-facing exam discovery, real fixed-form mock-test-attempt lifecycle (`correctIndex` never leaked). Real seeded IELTS Academic program, all four sections. A real, unrelated migration-sequence bug (RISK_REGISTER R-91) was root-caused, not fixed here (out of scope). T2: real RAG-grounded IELTS Writing/Speaking band scoring (new `'exam'` `AiRequestClass`, ADR-058), real objective Reading/Listening scoring against the section's own answer key, real overall band aggregation (mean of 4 sections, rounded to nearest 0.5), new `exam.mock_test.completed` event. T3: real `GET /v1/mock-test-attempts` historical listing and real `Certificate` issuance on completion (a practice score report, issued regardless of score, reusing exams.prisma's own already-designed verification-token mechanism since E4 T8)** |
| E20 | [Certificate System](epics/E20-certificate-system.md) | S | E8 -- **Epic complete, T1-T2 implemented and merged (2026-08-14). T1: real, shared `CertificateService` (ADR-059) extracted from E19 T3's own already-proven issuance logic; a new real Level-completion producer in `ExerciseAttemptsService`, reusing `maybeEmitLessonCompleted`'s own per-exercise-attempt-existence check one level up the hierarchy. Found and fixed a real regression this task's own second producer exposed in test infrastructure it doesn't own; found and tracked (RISK_REGISTER R-104) a confirmed pre-existing, unrelated `GamificationModule` test-quality issue. T2: real, public, rate-limited `GET /v1/certificates/verify/:token` (the exact "10/IP/5min" spec named at E4 T8) and real, authenticated `GET /v1/certificates` learner listing. A real, found-and-fixed RLS/tenant-context bug on the public verify route (the identical class E15 T2's `BillingService.hasEntitlement()` already hit) -- fixed via the same `SERVICE_ROLE_PRISMA_CLIENT` pattern** |
| E21 | [Mobile Application (Flutter parity)](epics/E21-mobile-application.md) | XL | E2–E17 — **design phase (2026-08-14): first single-pass design complete, not yet implemented. Real, load-bearing blocker found and named, not routed around: the Flutter SDK is not installed in this environment, so no Dart/Flutter code can be written and actually verified here — this pass covers IMPLEMENTATION_GUIDE.md phases 1–10 (design gates) only, `apps/mobile/` remains an empty placeholder. Scope bounded to PRD/ROADMAP's own "core learning loop" MVP definition (auth, course/exercise-attempt consumption with last-write-wins offline sync, vocabulary SRS review, gamification display, push notifications), explicitly deferring Speaking/Pronunciation/Writing/Exam-Prep/Billing/Admin mobile parity as real, separately-scoped follow-ups. Three real gaps found and designed around: no device-token-registration mechanism exists anywhere (ADR-060, new `DeviceToken` model + `POST /v1/notifications/device-tokens` + a new `FcmPushProvider` on `notification-service`'s already-real Worker), no offline-sync mechanism was ever specified beyond PRD's one-line requirement (ADR-062, `drift`-backed local cache + FIFO write-queue), and no Flutter state-management choice had ever been made (ADR-061, Riverpod). Awaiting Flutter tooling availability to proceed to implementation** |
| E22 | Security Hardening & Compliance Gate | L | E2, E4, E5, E15 |
| E23 | Public Beta Launch Readiness | M | All prior epics |

Each epic's objective, acceptance criteria, and top risk are cross-referenced against the module/ADR that motivates it above; the epic sequence is reviewed at the start of each sprint planning cycle and updated in place (not forked into a separate tracking document) so this table never drifts from what engineering is actually building.

**Growth/Enterprise-phase epics** (scoped in detail when their phase begins, named now for dependency awareness): E24 Community Platform (module 16), E25 AI Translation Camera (module 13), E26 Video Learning (module 14), E27 Teacher Marketplace (module 18, depends on the `TEACHER`-role decisions made in E2), E28 Enterprise LMS (module 20, depends on E22's RLS work), E29 Public API Platform (module 27, depends on API_GUIDELINES.md's versioning discipline having been followed since E2).

## Feature classification (MVP / V1.1 / V2 / Enterprise / Future Research)

| Feature / Module                                                    | Classification              | Rationale                                                                       |
| ------------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------- |
| User Identity Platform, mandatory admin MFA                         | **MVP**                     | Blocks everything; ADR-011                                                      |
| AI Language Assessment Engine                                       | **MVP**                     | Core value prop entry point                                                     |
| Personalized Learning Engine                                        | **MVP**                     | Retention mechanic                                                              |
| AI Teacher Platform (7 agents, Orchestrator handoff, RAG grounding) | **MVP**                     | Core value prop; ADR-007, ADR-008                                               |
| Course Management + AI-assisted content authoring                   | **MVP**                     | No product without content; required to hit 10-language scope affordably        |
| Vocabulary Intelligence (SRS)                                       | **MVP**                     | Low-cost, high-engagement                                                       |
| Speaking Practice, Pronunciation Lab                                | **MVP**                     | Signature differentiator                                                        |
| Listening, Reading, Writing, Story Generator                        | **MVP**                     | Core skill coverage                                                             |
| Gamification + anti-gaming safeguards                               | **MVP**                     | Core engagement loop; safeguards launch-blocking (RISK_REGISTER.md R-15)        |
| Subscription — Free/Premium, trial, cancellation                    | **MVP**                     | Revenue; billing needs an off-ramp to launch                                    |
| Analytics — CEFR-progression outcome measurement                    | **MVP**                     | Proves PRD.md §8's core success metric                                          |
| Admin Platform (core), Notification System                          | **MVP**                     | Operate content/support and retention at launch                                 |
| Security System (full, incl. RLS, MFA)                              | **MVP**                     | Non-negotiable                                                                  |
| Mobile Application (core loop)                                      | **MVP**                     | Category-standard usage pattern                                                 |
| AI Infrastructure, Internal Platform Services                       | **MVP**                     | Foundational                                                                    |
| Referral / viral growth loop                                        | **Version 1.1**             | High value, needs its own fraud-prevention design first (RISK_REGISTER.md R-15) |
| Cosmetic in-app purchases, gift subscriptions                       | **Version 1.1**             | Monetization upside, not core value prop                                        |
| Proactive AI coaching nudges, selectable AI teacher personality     | **Version 1.1**             | Engagement upside once baseline usage data exists                               |
| Automated re-assessment triggers                                    | **Version 1.1**             | Manual/user-triggered re-assessment is an acceptable MVP substitute             |
| Certificate System (public verification depth)                      | **Version 1.1**             | Nice-to-have proof point                                                        |
| Exam Preparation — full 6-program breadth                           | **Version 1.1**             | MVP ships 1–2 programs; breadth is a fast-follow                                |
| Full multi-device/offline conflict resolution                       | **Version 1.1**             | MVP ships simpler last-write-wins semantics                                     |
| AI Translation Camera, Video Learning                               | **Version 2**               | New modality, independent of core loop                                          |
| Community Platform (full: groups, voice rooms)                      | **Version 2**               | Meaningful moderation investment required first (RISK_REGISTER.md R-16)         |
| **Family plan**                                                     | **Version 2**               | Blocked on ADR-013's parental-consent flow                                      |
| Business plan / SSO                                                 | **Enterprise**              | Depends on E22 RLS work and SSO integration                                     |
| Enterprise LMS (full)                                               | **Enterprise**              | Depends on Business plan, reporting rollups                                     |
| Teacher Marketplace                                                 | **Enterprise** (or late V2) | Depends on `TEACHER` role, payments/payouts, content governance                 |
| Public API Platform                                                 | **Future Research**         | No committed demand signal yet; API_GUIDELINES.md written to not preclude it    |
| AI Avatar Teacher                                                   | **Future Research**         | Significant unproven infra investment (real-time video generation)              |
| White-labeling / custom branding                                    | **Future Research**         | Only relevant once Enterprise demand is validated                               |

## Phase 2 — Growth (Engagement, Content Breadth, Monetization Depth)

**Goal:** Deepen engagement and monetization once the core loop is validated; expand content modalities and community.

**In scope:**

- Module 13 — AI Translation Camera
- Module 14 — Video Learning System (YouTube/subtitle learning)
- Module 16 — Community Platform (groups, discussions, voice rooms, challenges) — moderation design (RISK_REGISTER.md R-16) precedes launch, not follows it
- Module 18 — Teacher Marketplace (teacher profiles, courses, revenue sharing, content-governance workflow)
- Module 19 — Exam Preparation — full breadth across all 6 exam programs
- Module 21 — Certificate System — public verification, richer achievement records
- **Module 22 — Family plan, gated on the parental-consent flow required by ADR-013 being fully built and tested**
- Referral/viral growth program (with fraud prevention designed before launch), gift subscriptions, cosmetic in-app purchases
- Full mobile offline conflict-resolution UX (superseding MVP's last-write-wins)
- Expanded language catalog beyond the 10 MVP launch languages; possible additional UI languages (PRD.md §5.1)

**Exit criteria:**

- Demonstrated retention lift from community/gamification deepening (cohort-measured, not assumed).
- Teacher Marketplace live with a defined revenue-share model and moderation/quality bar.
- Family plan's parental-consent flow independently verified against COPPA and relevant app-store child-safety policies before enabling for any user.
- Video/camera modules meeting the same loading/empty/error/success and accessibility bar as MVP modules (DESIGN_SYSTEM.md §5) — new modalities do not get a lower quality bar.

## Phase 3 — Enterprise & Platform

**Goal:** Open new revenue lines (B2B, marketplace-adjacent, API) once consumer product-market fit and operational maturity (SOC 2-track security posture, DEPLOYMENT.md observability maturity) support enterprise sales cycles.

**In scope:**

- Module 20 — Enterprise LMS (organization accounts, employee training, reporting, assignments, SCIM roster sync)
- Module 22 — Subscription Platform — Business plan, SSO (SECURITY.md §2)
- Module 17 — AI Avatar Teacher (real-time avatars, interactive virtual classroom)
- Module 27 — API Platform (public Language API / AI education API, developer ecosystem, built on API_GUIDELINES.md conventions)
- SOC 2 Type II certification pursuit (SECURITY.md §10)
- Multi-region infrastructure expansion, if justified by enterprise data-residency requirements (MULTITENANCY.md §5, DEPLOYMENT.md §9)

**Exit criteria:**

- Enterprise LMS live with at least reference-customer validation of reporting/compliance requirements.
- Public API has documented rate limits, authentication, and a defined support/versioning commitment (API_GUIDELINES.md §10).

## Cross-phase, always-on priorities

These are not phase-gated — they are continuously invested in from MVP onward:

- Security & compliance (SECURITY.md, MULTITENANCY.md) — no phase ships with known-open critical findings.
- AI quality evaluation and governance (AI_GOVERNANCE.md §3) — golden-set and factual-accuracy regression testing gates every prompt/model change, every phase.
- Accessibility (DESIGN_SYSTEM.md §5) — WCAG AA bar applies to every new module, every phase.
- Documentation currency (CLAUDE.md, CHANGELOG.md) — docs/ updates ship in the same PR as the architectural change they describe, every phase; significant changes get an ADR (DECISIONS.md).
- Risk register review (RISK_REGISTER.md) — reviewed at every phase transition, not only at MVP planning.
