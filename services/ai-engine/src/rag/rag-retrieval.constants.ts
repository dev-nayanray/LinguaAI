/**
 * Provisional — no production retrieval data exists yet to derive these
 * from, same honesty precedent as Memory Manager's own retrieval
 * constants (T6) and ADR-034's cost-breaker thresholds.
 */
export const RAG_RETRIEVAL_CANDIDATE_POOL_SIZE = 10;
export const RAG_RETRIEVAL_TOKEN_BUDGET = 800;
