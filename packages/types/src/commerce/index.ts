// Commerce bounded context (ARCHITECTURE.md §2.1). Intentionally empty at
// E1 — domain types (Subscription, Plan, Invoice, Payment, ...) land here
// starting Epic E2. This file exists so the @linguaai/types/commerce
// subpath resolves via the package's real "exports" map (T7 acceptance
// criteria).
export {};
