// Enterprise bounded context (ARCHITECTURE.md §2.1). Intentionally empty
// at E1 — domain types (Organization, SeatLicense, SsoConnection, ...) land
// here starting Epic E2. This file exists so the
// @linguaai/types/enterprise subpath resolves via the package's real
// "exports" map (T7 acceptance criteria).
export {};
