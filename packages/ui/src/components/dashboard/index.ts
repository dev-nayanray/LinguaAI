// E3 T9 — DESIGN_SYSTEM.md §4's Dashboard layouts category: a single
// shared dashboard-grid/widget primitive, composed differently per context
// rather than rebuilt per context.
export {
  DashboardGrid,
  type DashboardGridProps,
  type DashboardGridColumns,
} from './dashboard-grid';
export { Widget, type WidgetProps } from './widget';
